### Description
Include persisted reading storage directly backed by `ScratchFile` and similar components.

### Focused Areas
- Finish porting and testing:
  - `ScratchFile`
  - `ScratchFileBuffer`
  - File-backed I/O operations.

### Goals
Ensure file-backed implementations adapt smoothly. Incorporate all paired tests.